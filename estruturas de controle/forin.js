const notas = [2,5.6,8.6,10,4.5]

for (const i in notas) {
    console.log(i,notas [1])
    }
    //posicoes com os valores dos arrays
 
const pessoa ={
    nome: 'ana',
    sobrenome: 'maria',
    idade: 20,
    peso: 80
}
// criei um objeto
for (atributo in pessoa) {
    console.log(`${atributo}=${pessoa[atributo]}`)
    }
